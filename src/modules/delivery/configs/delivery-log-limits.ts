import { SEARCH_PAGE_SIZE } from "@/validators/pagination";

/** Max rows per delivery log list/export request (D-BE-07). */
export const DELIVERY_LOG_EXPORT_MAX_ROWS = 1000;

/** Default page size for delivery log list pagination. */
export const DELIVERY_LOG_DEFAULT_PAGE_SIZE = SEARCH_PAGE_SIZE;
